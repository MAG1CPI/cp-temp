/* Missing return stmt */

void func(int a, int b)
{
	int sum = a + b;
    if(a < 10)
        sum = sum + 1;
    else
        sum = sum + 1;
}

int main()
{
	if(func(1,2));
	return 0;
}