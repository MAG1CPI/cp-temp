int main()
{
    int a = 2, b = 5, c = 7;
    /*
    while (a < b && b < c)
    {
        //if (a < b)
        //    break;
        a = b + a;
    }
    */
    if(a < b && b < c)
        a = b + a;
    return a;
}
